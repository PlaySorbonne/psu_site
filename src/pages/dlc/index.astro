---
import { rawMDtoSortedArray, type Slide } from "@/helpers";
import FrontPage from "@/layouts/FrontPage.astro";

const slides = rawMDtoSortedArray(await Astro.glob<Slide>("./*.md"));
---

<FrontPage title="Play Sorbonne le DLC" logo="dlc" slides={slides}>
  <center>
    <h1>En Construction</h1>
  </center>
</FrontPage>

---
import { rawMDtoSortedArray, type EventT } from "@/helpers";
import FrontPage from "@/layouts/FrontPage.astro";

const events = rawMDtoSortedArray(await Astro.glob<EventT>("./*.md"));
---

<FrontPage title="Play and Learn Society" logo="pls" events={events} />
